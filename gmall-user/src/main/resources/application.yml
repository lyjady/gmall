server:
  port: 12010
  servlet:
    context-path: /user
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gmall?characterEncodint=UTF-8
    username: root
    password: 123456
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
  mvc:
    date-format: yyyy-MM-dd HH:mm:ss
mybatis:
  mapper-locations: classpath:/com/augustus/user/mapper/*.xml
  type-aliases-package: com.augustus.user.bean
logging:
  level:
    com.augustus.user.mapper: debug